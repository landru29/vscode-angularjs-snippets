<snippet>
    <prefix>ngfactory</prefix>
    <description>AngularJs Factory</description>
    <body>
        <![CDATA[
angular.module("${Module}").factory("${Factory}", function ${Factory} (${Dependencies}) {
    "use strict";

    /**
     * @ngdoc object
     * @name ${Module}.object:${Factory}
     * @description
     * <p>${Description}</p>
     * @param {object} opts options
     * <ul>
     *     <li><strong>foo</strong> <em>[optional]</em> this is foo</li>
     * </ul>
     */
    var ${Factory} = function ${Factory} () {
        $0
    };

    /**
     * @ngdoc function
     * @methodOf ${Module}.object:${Factory}
     * @name ${fn}
     * @description ${fn} description
     * @param {String} data ${fn} data
     */
    ${Factory}.prototype.${fn} = function ${fn} (data) {
    };

    return ${Factory};
});
        ]]>
    </body>
</snippet>